{% for status in statuses %}
<p>{{ status.name }}
    <button onclick="location.href='{{ path('editStatus', {'id':status.id}) }}'">Edytuj</button>
    <button onclick="location.href='{{ path('removeStatus', {'id':status.id}) }}'">Usuń</button>
</p>
{% endfor %}